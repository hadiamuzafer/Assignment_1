//Q16
let guestlist: string[] = ["Fatima, "Malika", "Mahreen",Arooj];

for (let guestlists Of guestlist){
console.log(`${guestlists} We found a bigger dinner table`);}

// Add a new guest to the beginning of the array
guests.unshift("hifza");

// Add a new guest to the middle of the array
const middleIndex = Math.floor(guests.length / 2);
guests.splice(middleIndex, 0, "zainab");

// Add a new guest to the end of the array using append()
guests.push("hadi");

// Print invitation messages
guestlist.forEach((guest:string)=>{
console.log(${guest} You Are Invited For A Dinner})
